rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access is via firebase-admin SDK (server-side),
    // which bypasses security rules. Deny all client-side access.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
